import { Bullpen } from './Bullpen';

const meta = {
    component: Bullpen,
    parameters: {
        // docs: { disable: true },
        layout: 'fullscreen',
        viewMode: 'story',
        previewTabs: {
            'storybook/docs/panel': { hidden: true }
        }
    }
};

export default meta;

export const Primary = {};
