import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import * as stories from './Link.stories.tsx';

<Meta of={stories} />

# Link

Use Link when the result of the action is the member being taken to another place within the app.

<Canvas of={stories.Default} />

<ArgTypes of={stories.default.component} />

## Unstyled

The unstyled option will remove the default link styling to allow Link to be used semantically around different types of elements.

<Canvas of={stories.Unstyled} />

## Target

Use `target` to control link opening behavior.

<Canvas of={stories.Target} />

## Click event

Link has an `onClick` to allow an action before the navigation event.

<Canvas of={stories.ClickEvent} />
