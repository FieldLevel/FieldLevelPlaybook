import { Meta, Canvas, Controls } from '@storybook/blocks';

import * as stories from './Subheading.stories.tsx';

<Meta of={stories} />

# Subheading

Combine Subheading with Heading to create information hierarchy on a page.

<Canvas of={stories.Default} />

<Controls of={stories.Default} />
