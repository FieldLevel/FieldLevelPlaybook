import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { ChoiceGroup } from '@fieldlevel/playbook';
import * as stories from './ChoiceGroup.stories.tsx';

<Meta title="Forms/ChoiceGroup" component={ChoiceGroup} />

# ChoiceGroup

Use to create a list of radio buttons or checkboxes. Use a Choice Group when you want to emphasize options, when you have less than five options, when the comparison of options needs to be clear, and when a quick response is a priority. Choice Groups are generally easier to use in a mobile-first experience, choose the list when usage is unclear.

<Canvas>
    <Story story={stories.Default} name="Default" />
</Canvas>

<ArgsTable story="Default" />

## Single Choice

<Canvas>
    <Story story={stories.SingleChoice} />
</Canvas>

## Multiple Choice

<Canvas>
    <Story story={stories.MultiChoice} />
</Canvas>

## Disabled

<Canvas>
    <Story story={stories.Disabled} />
</Canvas>

## Disabled Choice

<Canvas>
    <Story story={stories.DisabledChoice} />
</Canvas>

## Spacing

Loose spacing is available when the ChoiceGroup is being used on it's own instead of as a form element.

<Canvas>
    <Story story={stories.Spacing} />
</Canvas>

## With Error

<Canvas>
    <Story story={stories.WithError} />
</Canvas>
