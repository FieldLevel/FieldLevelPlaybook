import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import { FormLayout } from '@fieldlevel/playbook';
import * as stories from './FormLayout.stories.tsx';

<Meta of={stories} />

# FormLayout

Use Form Layout to organize inputs using a six-column grid. Fields can occupy the entire row (full) all the way down to a single column (oneSixth).

<Canvas of={stories.Default} />

<ArgTypes of={FormLayout}/>

## FormLayout.Item

<ArgTypes of={FormLayout.Item} />

## Row

<Canvas of={stories.Row} />

## Row With Empty Space

<Canvas of={stories.RowWithEmpty} />
