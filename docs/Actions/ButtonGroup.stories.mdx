import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { ButtonGroup } from '@fieldlevel/playbook';
import * as stories from './ButtonGroup.stories.tsx';

<Meta title="Actions/ButtonGroup" component={ButtonGroup} />

# ButtonGroup

Button Group displays multiple related actions in a horizontal row to help with arrangement and spacing.

<Canvas>
    <Story story={stories.Default} name="Default" />
</Canvas>

<ArgsTable story="Default" />

## Spacing

Use tight spacing with slim buttons.

Use base spacing with base buttons.

Use loose spacing with large buttons.

<Canvas>
    <Story story={stories.Spacing} />
</Canvas>

## Distribute

<Canvas>
    <Story story={stories.Distribute} />
</Canvas>
