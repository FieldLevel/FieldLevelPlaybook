import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { LazyImage } from '@fieldlevel/playbook';
import * as stories from './LazyImage.stories.tsx';

<Meta title="Images and Icons/LazyImage" component={LazyImage} />

# LazyImage

An LazyImage is an image that supports lazy loading, with a fallback mechanism for browsers that don't support native lazy loading.

<Canvas>
    <Story story={stories.Default} name="Default" />
</Canvas>

<ArgsTable story="Default" />

## Lazy

<Canvas>
    <Story story={stories.Lazy} />
</Canvas>
