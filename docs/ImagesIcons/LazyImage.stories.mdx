import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { LazyImage } from '@fieldlevel/playbook';
import * as stories from './LazyImage.stories.tsx';

<Meta title="Images and Icons/LazyImage" component={LazyImage} />

# LazyImage

A LazyImage is an image that supports lazy loading, with a fallback mechanism for browsers that don't support native lazy loading.

<Canvas>
    <Story story={stories.Default} name="Default" />
</Canvas>

<ArgsTable story="Default" />

## Cover

Use to control whether the image content is letterboxed to fit inside the content box or sized and cropped fill the entire content box with no whitespace.

<Canvas>
    <Story story={stories.Cover} />
</Canvas>
